import numpy as np

'''
Running modes:

* run a trained policy and generate historical data
* run a policy with live training from historical data
* run a policy with live training using an asynchronous running simulation
'''
